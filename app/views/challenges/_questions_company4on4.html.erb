
<div class="collapse" id="questions-company_4on4" data-parent="#accordion">

  <div class="d-flex flex-row bd-highlight mb-3 align-items-center" >
    <div class="p-2 bd-highlight">
      <h3> #4. Which questions will you be asking to your interviewer?</h3>
    </div>
    <div class="p-2 bd-highlight">
      <%= simple_form_for @interview_preparation  do |f| %>
        <%= button_tag type: 'submit', class: "btn btn-flat" do %>
            Save
            <i class="far fa-check-circle" aria-hidden="true"></i>
        <% end %>
      </div>
  </div>

    <div class="form-group ">
      <div>
        <%= f.simple_fields_for :candidate_works, @questions4on4 do |cw| %>
          <%= cw.input :question, value: cw.object.question, label: false, disabled: true %>
          <%= cw.input :answer, as: :text, label: false, placeholder: "Showing I did my research but still want the interviewer's view upon it" %>
        <% end %>
      </div>
    </div>
  <% end %>

</div>
